# Test dependencies — install alongside requirements.txt
-r requirements.txt

pytest==8.2.0
pytest-asyncio==0.23.7
pytest-mock==3.14.0
pytest-cov==5.0.0
httpx==0.27.0           # already in main deps; pinned here for clarity
