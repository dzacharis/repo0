# PLACEHOLDER — never commit real values.
# Create with:
#   kubectl create secret generic transform-hub-secrets \
#     --from-literal=keycloak-admin-client-secret='<secret>' \
#     --namespace apps
apiVersion: v1
kind: Secret
metadata:
  name: transform-hub-secrets
  namespace: apps
type: Opaque
stringData:
  keycloak-admin-client-secret: "CHANGE_ME"
